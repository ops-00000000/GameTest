{
  "name": "chess-roguelike",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "cd server && npx wrangler dev",
    "dev:client": "cd client && npm run dev",
    "build:shared": "cd packages/shared && npm run build",
    "build": "npm run build:shared && cd client && npm run build",
    "deploy": "npm run build:shared && cd client && npm run build && cd ../server && npx wrangler deploy"
  }
}